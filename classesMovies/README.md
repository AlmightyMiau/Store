# Movie Database

make executable with `g++ *.cpp -o main`
run with `cat input.txt | ./main`

change movies by changing input.txt
alternatively, you can run `./main` and input data manually, but formatting will be weird
